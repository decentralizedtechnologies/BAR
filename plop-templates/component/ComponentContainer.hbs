

import { {{{pascalCase name}}}Props } from './{{{pascalCase name}}}.types';
import { {{{pascalCase name}}} } from './{{{pascalCase name}}}';

export const {{{pascalCase name}}}Container = ({className, style}: {{{pascalCase name}}}Props) => {
  return (
    <{{{pascalCase name}}} className={className} style={style}>Test</{{{pascalCase name}}}>
  );
};
